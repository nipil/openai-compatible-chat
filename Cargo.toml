[workspace]
members = ["ai_model_info", "native", "portable", "wasm"]
resolver = "2"

[workspace.dependencies]
portable = { path = "portable" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
strum = { version = "0.28", features = ["derive"] }
